# TODO
- [ ] 0x的语义歧义, 语法高亮下？ 通过空格区分？ 0x表示16进制， 0 x 表示乘法。
- [ ] 快照图标bug, win10?
- [ ] 计算值和显示值分开
- [ ] 时区支持，日期和时间的显示逻辑在优化一下。设置时区，`> @8`，`> @-8`

“大写人民币”这个插件的大部分使用场景应该会与财务工作有关，可参考财政部发布的《会计基础工作规范》中对大写金额的书写要求。

改进设置
- 快捷运算进入插件还是复制结果
- 可以设置^用作幂运算，此时如何处理异或？提供 ^^ 以及 xor() 函数
- 设置小数位数
- 输出设置为2进制，8进制, 16进制或者中文大写

- [1.1 2.1 3.1] 支持小数
- 所有core中新增功能，都需要先确定case, 确定边界，并在tests中生成测试用例
- 输出过长如何处理？

## RoadMap 
### V3.0
**界面改进**
- 设置持久化保存，支持设置输出格式，小数位数，科学计数法，十六进制

### V3.2
**自定义函数**
-? 添加性能监控, 计算超时返回 throw new Error('计算超时');
- createLambdaFunction 不是把参数对象传给求值器，而是先做字符串替换，AST 级参数绑定/求值（不经字符串）才是长期最稳方案

### V3.3
to be determined